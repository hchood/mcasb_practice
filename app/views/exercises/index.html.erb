<h3>Exercises</h3>

<table>
  <thead>
    <tr>
      <td>Name</td>
      <td>Description</td>
    </tr>
  </thead>
  <tbody>
    <% @exercises.each do |exercise| %>
      <tr>
        <td><%= exercise.name %></td>
        <td><%= exercise.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @exercises %>
